Generate OPA/Rego policy text for IaC policy checks.
Requirements:
- Include deny rules tied to findings
- Include traceability reference in each deny message
- Keep policy deterministic and dependency-free
